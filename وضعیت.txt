══════════════════════════════════════════════════════════════════════════════
وضعیت پروژه گاردبات / نکسوس — کانتکست صوتی (Voice Context)
تاریخ: ۲۰۲۶-۰۹-۲۶
شاخه: main
کامیت کد: <پس از کامیت ثبت می‌شود>  (base: 46314db)
سند مرجع فنی: AgentMD.md §54.34  |  §53.14 / §53.11 / §53.8 / §53.12 (قواعد)
══════════════════════════════════════════════════════════════════════════════


۱) خلاصهٔ اجرایی
────────────────
یک ویس (پیام صوتی) تلگرامی که خطاب به نکسوس باشد، حالا **با صدا** جواب می‌گیرد —
و آن جواب از **همان زمینه‌ای** ساخته می‌شود که یک پیام متنی می‌گرفت. این «ربات
صوتی دوم» نیست: ویس اول توسط همان مسیر موجود **فهمیده** می‌شود (دانلود،
رونویسی، هویت فرستنده، لبهٔ ریپلای و هدف، اتاق/آگاهی، حافظه، وضعیت گفتگو،
تاریخ، نتیجهٔ جست‌وجو) و تازه بعد از آن، آن زمینهٔ ساخته‌شدهٔ سمت سرور به‌همراه
صدای خودِ شخص به Live API داده می‌شود؛ یک نوبت، و جواب به‌صورت پیام صوتی که
**زیر همان ویس** ریپلای می‌شود برمی‌گردد.

حالت عادی دست‌نخورده است: با خاموش بودن لایه، ویس دقیقاً مثل قبل رونویسی و
متنی جواب داده می‌شود. هر خطایی هم به مسیر متنی برمی‌گردد، پس کسی بی‌جواب
نمی‌ماند.

نتیجهٔ تست: ۴۲۰۱ تست، ۰ خطا. pyflakes روی همهٔ فایل‌های دست‌خورده پاک است.


۲) تصمیم‌های ریشه‌ای (و چرا)
─────────────────────────────
  (الف) زمینه را main.py می‌سازد، نه این ماژول.
        طرح وسوسه‌انگیز این است که یک نشست Live باز شود و مدل حرف بزند. رد شد:
        نشست لخت هیچ نمی‌داند کی صحبت می‌کند، به چه چیزی ریپلای شده، اتاق چه
        می‌گوید و چه چیزی دربارهٔ او به یاد دارد — و اگر این‌ها را «از خود مدل»
        بپرسیم، هویت و حافظه می‌شوند خروجیِ مدل. سرور این‌ها را دقیقاً مثل یک
        نوبت متنی می‌سازد و نشست Live فقط مصرف‌کننده است.

  (ب) یک «نوبت»، نه یک «تماس».
        LiveTurn وصل می‌شود، بلوک زمینه را می‌فرستد، گفتار + سکوت پایانی را
        می‌فرستد، صدای کراندار را می‌خواند و **در هر مسیری می‌بندد**. روی همان
        transport تماس ساخته شده و هیچ چیز دیگری با session.py مشترک ندارد:
        نه کانال تلگرام، نه نقشهٔ گویندگان، نه صف پخش، نه barge-in، نه reconnect.

  (ج) سکوت پایانی اجباری است.
        provider پایان یک گفته را در سکوتی که *بعد از آن* می‌آید پیدا می‌کند.
        این همان یافته‌ای است که پمپ سکوت تماس برایش وجود دارد؛ نوبتی که فقط
        گفتار را بفرستد هیچ جوابی نمی‌گیرد.

  (د) workload مستقل در pool.
        همان قابلیت provider و به‌طور پیش‌فرض همان کلید تماس، اما سهمیه و
        breaker مستقل: تماس با «چند تا در روز باز می‌شود» سهمیه‌بندی می‌شود و
        ویس با «چند تا در روز جواب می‌دهد». یک بودجهٔ مشترک باعث می‌شد یک
        بعدازظهرِ شلوغ ویس، روزِ یک تماس در انتظار را خرج کند.


۳) تغییرات انجام‌شده
────────────────────
  ▸ ماژول ویژگی — app/voice_context.py (جدید)
      - سوئیچ پایدار (running/configured/enabled/available) + برچسب وضعیت.
      - named() و command_from(): واژگان خودِ لایه فقط وقتی خوانده می‌شود که
        لایه نام برده شده باشد — این بررسی داخل خود تابع است، نه بر عهدهٔ
        فراخوان. به همین دلیل «باز»/«بسته» و «on»/«off» خالی اینجا امن‌اند.
      - instruction() = chat.SYSTEM_INSTRUCTION + SPOKEN_ADDENDUM (همان پرسونا،
        فقط قواعدِ رسانه: بدون Markdown/لینک/شناسه، روایت‌نکردنِ رسانه، طول
        هنوز به‌اندازهٔ درخواست).
      - Answer + answer(): decode (خارج از event loop) → lease از pool →
        LiveTurn زیر سمنفور → encode به OGG/Opus. هرگز raise نمی‌کند جز
        CancelledError.
      - accepts() برای کران‌های ورودی؛ status() بدون کلید و بدون متن.

  ▸ نوبت Live — app/voice_live/turn.py (جدید)
      - LiveTurn.run(): تلاش مجدد به‌صورت کل نوبت، فقط برای خطاهای retryable؛
        هر تلاش یک نشست **تازه** (factory، نه transport).
      - سقف پاسخ بر حسب ثانیهٔ گفتار؛ قطع می‌کند و capped را گزارش می‌دهد.
      - _context_block(): زمینهٔ سرور + رونویسی سرور، با صراحت «این سند سرور
        است، نه دستور».

  ▸ کدک‌ها — app/voice_live/audio.py
      - decode_to_pcm16 و encode_pcm24_to_ogg و pcm_seconds اضافه شد؛
        chat._pcm_to_ogg حالا به همان encode واگذار می‌کند تا بیت‌ریت و
        کانتینر در یک جا تعیین شود.

  ▸ transport — app/voice_live/gemini_live.py
      - send_context(text, *, turn_complete=False): پیش‌فرض همان چیزی است که
        refresh تماس می‌خواهد؛ True برای نوبتی که هیچ گفتاری ندارد (بلوک
        خودش پیام است). مسیر تماس دست‌نخورده.

  ▸ سوئیچ پایدار — app/db.py
      - جدول voice_context_control (تک‌ردیفی) + get/set/reset، دقیقاً با شکل
        search_control. «دست‌نخورده» یعنی روشن.

  ▸ مجوز و ابزار — app/admin_service.py و app/admin_tools.py
      - دو عمل voice_context_offline/online با مجوز nexus.control (هیچ
        role bundle آن را ندارد)، kind=OP_SYSTEM، requires_nexus_online=False،
        needs_confirmation=True. set_running هیچ بررسی مجوزی ندارد؛ تنها
        مرجع، admin_service.execute است.

  ▸ مسیر گفتگو — app/main.py
      - _prepare_conversation_media حالا ۶-تایی برمی‌گرداند و بایت‌های ویس را
        هم می‌دهد (یک دانلود، نه دو).
      - شاخهٔ Voice Context در _answer_conversationally بعد از ساخته‌شدن context
        و قبل از chat_queue.reply؛ زیر chat_queue.serialized(room, user) تا با
        نوبت‌های متنی همان گفتگو مرتب شود (قفل غیربازگشتی است و قبل از fallback
        آزاد می‌شود).
      - نوبتی که جواب تولید کرده، جوابش را می‌فرستد و **مدل متنی را دوباره
        صدا نمی‌زند**؛ فقط نوبتی که هیچ تولید نکرده به مسیر متنی می‌افتد.
      - _owner_state_command: لایهٔ voice_context هم اضافه شد (یک متغیر layer
        که عمل، جمله‌ها و برچسب «از قبل» را از یک جا می‌خواند).
      - _nexus_status_text: خط «کانتکست صوتی».

  ▸ صف — app/chat_queue.py
      - serialized(): همان قفل reply، به‌صورت context manager، برای نوبتی که از
        chat.reply نمی‌گذرد.

  ▸ تنظیمات — app/config.py
      - بلوک کامل Voice Context + ورودی pool با capabilities
        {audio_in, audio_out, live}، retries=0، daily_budget=300.

  ▸ تشخیص — app/agent_data.py
      - voice_context_enabled در nexus_diagnostics (وضعیت مؤثر، نه فقط پیکربندی).


۴) فایل‌های تغییریافته
────────────────────────
  کد:
    app/voice_context.py          (جدید)
    app/voice_live/turn.py        (جدید)
    app/voice_live/audio.py       app/voice_live/gemini_live.py
    app/chat.py  app/chat_queue.py  app/config.py  app/db.py  app/main.py
    app/admin_service.py  app/admin_tools.py  app/agent_data.py
  تنظیمات:
    .env.example  (بلوک VOICE_CONTEXT_*)
  تست:
    tests/test_voice_context.py   (جدید، ۵۳)
    tests/conftest.py  tests/test_nexus.py  tests/test_chat_daily_budget.py
    tests/test_ai_admin.py  tests/test_ai_isolation.py  tests/test_chat_latency.py
    tests/test_awareness_latency.py  (سقف اعلان ابزار: ۶۲۰۰۰ → ۶۶۰۰۰ با دلیل)
  مستندات:
    AgentMD.md (§53.14، §54.34)
    README.md (بخش «Voice messages»)
    ChatGPT.md (§10 همگام شد)


۵) معماری (خلاصه)
──────────────────
  ویس خطاب‌به‌نکسوس
    → دروازهٔ اتاق (authorized_groups) — بدون تغییر
    → هویت + آگاهی + حافظهٔ نام — بدون تغییر
    → دانلود + رونویسی (_prepare_conversation_media)؛ بایت‌ها هم نگه داشته می‌شوند
    → خواندن زمینه (context_plan.read) → ترکیب context_plan.compose
    → یادداشت مالک + رابطهٔ سرور
    → [اگر لایه روشن و اعتبار موجود]
         قفل گفتگو → voice_context.answer
             decode OGG→PCM → lease از pool voice_context
             → LiveTurn: connect → بلوک زمینه → گفتار + سکوت → خواندن کراندار → بستن
             → encode PCM→OGG
         → _send_voice(..., destination) که پیش‌فرضش همان پیام ویس ورودی است
      [در غیر این صورت / در هر خطا] → مسیر متنی قبلی، بدون تغییر

  قواعد دست‌نخورده:
    - نشست صوتی هیچ ابزاری ندارد؛ اختیار فقط در admin_service است.
    - پرسونا یک منبع رفتاری است؛ SPOKEN_ADDENDUM فقط رسانه را عوض می‌کند.
    - جداسازی مستأجر، سهمیهٔ آگاهی، صف متنی و همهٔ سوئیچ‌های قبلی دست‌نخورده.
    - fail-open: هیچ خطای صوتی باعث کرش یا خرابی حافظه نمی‌شود.


۶) نتیجهٔ تست‌ها
─────────────────
  کل مجموعه:  ۴۲۰۱ passed / ۰ failed
  pyflakes:   روی همهٔ فایل‌های دست‌خورده پاک.
  اسکن راز:   diff ایندکس‌شده پاک است (بدون کلید Gemini/Tavily/توکن).


۷) کامیت، شاخه و دیپلوی
────────────────────────
  شاخه:            main
  base:            46314db
  کامیت‌های این کار:  <پس از کامیت تکمیل می‌شود>
  پوش:             origin/main و dashmo3i/main — با git ls-remote روی خود ریموت
                   تأیید می‌شود، نه با خروجی git push.
  تصویر دیپلوی:     <پس از دیپلوی>
  تصویر رول‌بک:     <پس از دیپلوی>

  توجه: ساخت ایمیج از **git archive HEAD** انجام می‌شود، نه از working tree —
  چون یک نشست هم‌زمان تغییرات uncommitted در app/voice_live/{session,telegram_voice}.py
  دارد که متعلق به این کار نیست و نباید دیپلوی شود (همان احتیاطی که §54.33 کرد).


۸) پروب زندهٔ سرتاسری
──────────────────────
  <پس از دیپلوی ثبت می‌شود>


۹) چه چیزی لایو است و چه چیزی در انتظار
────────────────────────────────────────
  <پس از دیپلوی ثبت می‌شود>

  در انتظار / خارج از این کار:
    • داشبورد (Admin Control Center) مراحل M4…M8 — محصول بعدی، طبق §54.27.
    • تغییرات uncommitted در app/voice_live/* و tests/test_voice_live_* که
      متعلق به این کار نیستند و عمداً کامیت نشدند (Voice Live توسط مالک رها
      شده است؛ §54.33).
    • روتین نگهداری دیسک و سایر کارهای سرور، دست‌نخورده.


۱۰) محدودیت‌های باقیمانده (صادقانه)
────────────────────────────────────
  • سقف پاسخ بر حسب ثانیهٔ گفتار است (پیش‌فرض ۱۲۰) و در LiveTurn یک کفِ یک
    ثانیه دارد؛ سقف کمتر از یک ثانیه قابل تنظیم نیست. سقف می‌بُرد و capped را
    گزارش می‌کند؛ اگر صدا قابل ارسال نباشد، متن رونویسی به‌عنوان متن می‌رود.
  • تشخیص گفتار خودِ provider در نام‌های فارسی خطا می‌کند — اندازه‌گیری شد،
    «نکسوس» به‌صورت «نیکسوس» برگشت — و دقیقاً برای همین رونویسی سرور به‌عنوان
    بلوک زمین‌گیرکننده کنار صدا فرستاده می‌شود.
  • یک ویس نمی‌تواند لنگر منشن تلگرام را حمل کند، پس دستور «تگ کن» مسیر متنی
    را نگه می‌دارد؛ پاسخ صوتی نمی‌تواند شخصِ خواسته‌شده را notify کند.
  • لایه به‌طور پیش‌فرض کلید Live را به اشتراک می‌گذارد؛ جداسازی، «workload»
    است (سهمیه + breaker)، نه کلید.
  • VOICE_CONTEXT_SEND_AUDIO=false (حالت فقط‌متنی) پوشش تست دارد اما چیزی نیست
    که پروب زنده تمرین کند.
