══════════════════════════════════════════════════════════════════════════════
وضعیت پروژه گاردبات / نکسوس — بازنگری ۲۱ بخشی
تاریخ: ۲۰۲۶-۰۹-۲۶
شاخه: main
کامیت کد: 01725c9  (base: fd4f9d6)
سند مرجع فنی: AgentMD.md §54.31  |  §53.7 / §53.8 / §53.12 (قواعد)
══════════════════════════════════════════════════════════════════════════════


۱) خلاصهٔ اجرایی
────────────────
هر ۲۱ بخش در یک پاس انجام شد. هیچ بخشی ناتمام نمانده و هیچچیز «برای بعد»
کنار گذاشته نشده است. چهار مشکلِ گزارششدهٔ مالک، هر کدام تا ریشهٔ واقعی در کد
ردیابی شد (نه حدس)، و بعد اصلاح شد:

  • پیام محدودیت نرخ («داری خیلی سریع پیام میدی») → حذف شد و معماری صف اضافه شد.
  • «فلانی رو تگ کن» جوابِ خودِ فرمانده را میداد → حالا هدف واقعی را پیدا میکند.
  • جوابها خلاصه میشدند → سقف توکن بالا رفت، قاعدهٔ «طول بهاندازهٔ سؤال» شد،
    و پاسخ بلند بهجای بریدهشدن به چند پیام تقسیم میشود.
  • اسم کاربرهای گروه را نمیشناخت → حافظهٔ نامِ اتاق اضافه شد.

نتیجهٔ تست: ۴۱۱۴ تست، ۰ خطا. pyflakes روی همهٔ فایلهای دستخورده پاک است.


۲) مشکلات گزارششده و ریشهٔ هرکدام (تشخیص قبل از تغییر)
───────────────────────────────────────────────────────
  (الف) «داری خیلی سریع پیام میدی»
        ریشه: جملهٔ خودِ ما بود، در chat._MESSAGES. پشتش هیچ صفی نبود: یا پیام
        به پنجرهٔ داخلی میخورد و با پیام رد رد میشد، یا همزمان اجرا میشد و دو
        پیام تاریخچهٔ یکسان میخواندند و جواب همدیگر را میدادند.

  (ب) «فلانی رو تگ کن» → جواب برای خودِ فرمانده
        ریشه: واژهٔ «تگ» هیچجای واژگان دستوری نبود، پس هدف به هیچکس resolve
        نمیشد و مقصد همان پیام فرمانده میماند.

  (ج) خلاصهنویسی
        ریشه: دو علت. اول، پرسونا «دو-سه جمله» را بهعنوان قاعدهٔ ثابت گفته بود.
        دوم، max_output_tokens=1024 وسط فکر جواب را میبُرید. بعد هم _fit_reply
        متن را به GEMINI_CHAT_REPLY_CHARS کوتاه میکرد.

  (د) نشناختن اسم کاربران
        ریشه: تنها اسمهایی که به مدل میرسید، اسم گویندگانِ پنجرهٔ اخیر بود.
        هیچ خوانندهای برای «این پیام دارد چه کسی را نام میبرد» وجود نداشت.


۳) تغییرات انجامشده
────────────────────
  ▸ صف نوبت — app/chat_queue.py (جدید)
      - قفل per-(chat_id, user_id): نوبتهای یک گفتگو سریال میشوند تا دو پیام
        همزمان تاریخچهٔ یکسان نخوانند و جواب همدیگر را ندهند.
      - سمنفور سراسری با GEMINI_CHAT_MAX_CONCURRENCY: همزمانی کراندار.
      - محدودیت نرخِ خودِ ما «صبر» میشود (backoff با jitter تا
        GEMINI_CHAT_QUEUE_MAX_WAIT) و پیام رد نمیشود؛ بعد از مهلت، سکوت.
      - chat._MESSAGES دیگر جملهای برای rate_limit / user_rate_limit ندارد،
        پس ChatReply.message خالی است.
      - محدودیتهای نرخ سمت provider همچنان کار gemini_pool است؛ صف آنها را
        هرگز نمیبیند.

  ▸ هدفِ پاسخ — app/reply_target.py
      - واژگان تگ به واژگان دستوری اضافه شد: «تگ کن»، «منشن کن»، «صداش کن»،
        «خطاب کن»، «ادش کن» …
      - Candidate؛ و روی Target: ambiguous / ambiguous_query / wants_mention /
        no_reply.
      - _name_lookup وضعیت ok در برابر ambiguous را حفظ میکند.
      - شخص نامبرده که پیامی در پنجره ندارد، «بیضمیمه» (unattached) ارسال
        میشود، نه زیر پیام پرسشگر.

  ▸ مسیر پاسخ — app/main.py
      - _send_chat(..., mention=...): لنگر tg://user?id=… را خودِ سرور تزریق
        میکند (شناسه از سرور، نام HTML-escape شده).
      - _ambiguous_target_text: سؤال «کدامیک؟» از کاندیدهای سرور، بدون هیچ
        فراخوانی مدل.
      - _people_context / _render_roster: حافظهٔ نامِ اتاق.
      - _split_for_telegram / _seam: پاسخ بلند در درزهای طبیعی به چند پیام
        تقسیم میشود؛ مقصد پاسخ و منشن فقط روی پیام اول.

  ▸ لحن و طول — app/chat.py
      - پرسونا یکبار و منسجم بازنویسی شد: گرمتر و محاورهایتر؛ طول «بهاندازهٔ
        سؤال» و صریحاً «هرگز سقف نیست»؛ آینهکردن لحن (شامل بازنویسی صریح مالک
        برای فحش جدی)؛ مرزهای سخت صریح.
      - _fit_reply دیگر سیاست طول نیست؛ فقط نگهبان runaway است
        (GEMINI_CHAT_REPLY_MAX_CHARS = 12000).
      - max_output_tokens: 1024 → 8192 (فقط توکنهای تولیدشده حساب میشوند، پس
        جواب کوتاه همانقدر هزینه دارد).

  ▸ خواندن طولِ درخواست — app/answer_shape.py (جدید)
      - تشخیص قطعیِ full / news / short / هیچ، از واژگان بسته؛ بهعنوان
        «خواندن سرور» کنار تاریخ در context تزریق میشود.

  ▸ حافظهٔ نام — app/people.py
      - roster(chat_id, text=...): اشخاصی که پیام واقعاً نامشان را برده، با
        همان مقایسهٔ دقیق و نرمالشدهٔ resolve؛ @ هم تا میشود تا «@milad» بیاید.
      - دو بار کراندار + NEXUS_PEOPLE_ROSTER_SCAN. اندازهگیری شد: اسکن نامحدود
        اتاق ۵۰۰۰ نفره ~۹۰ms بهازای هر پیام در برابر ~۱۰ms برای ۵۰۰ نفر آخر.

  ▸ ترکیب context — app/context_plan.py
      - اسلات مستقل people بین target و room. عمداً داخل بلوک اتاق ادغام
        نشد: اتاق متنِ بالادست برای حذف تکراری است، پس شخصی به نام «Python»
        باعث میشد خطِ حافظهٔ «Python» تکراری شمرده و حذف شود.

  ▸ آگاهی — app/db.py و app/awareness.py
      - ستون group_messages.username (افزایشی، _ensure_column، پیشفرض '').
      - از user.username ثبت و کنار شناسه رندر میشود:
        [member] میلاد (222) @milad: …
        تا دو عضو همنام از هم قابل تشخیص باشند. خط بدون username دقیقاً همان
        قبل است.

  ▸ جستوجو — app/web_search.py و app/config.py
      - ۸ نتیجه، GEMINI_SEARCH_SNIPPET_CHARS=600، GEMINI_SEARCH_MAX_CHARS=3600.
      - اهرم، «یافتهها» است نه درخواست دوم: یک فراخوانی Tavily همهٔ ۸ نتیجه را
        برمیگرداند، پس منبعِ سهمیهبندیشده (تعداد درخواست) دستنخورده است.

  ▸ یک نقص که pyflakes پیدا کرد، نه بریف — app/rbac.py
      - ماژول در مسیر «ادمینهای ذخیرهشده خوانده نشد» log.exception صدا میزد
        بدون آنکه logger تعریف شده باشد؛ یعنی همان افتِ مهربانی که کامنت خودش
        وعده میدهد، در عمل NameError میداد. دو خط: import logging و logger.


۴) فایلهای تغییریافته
────────────────────────
  کد:
    app/chat_queue.py        (جدید)
    app/answer_shape.py      (جدید)
    app/chat.py  app/config.py  app/main.py  app/reply_target.py
    app/people.py  app/context_plan.py  app/db.py  app/awareness.py
    app/web_search.py  app/rbac.py
  تنظیمات:
    .env.example   (GEMINI_CHAT_REPLY_MAX_CHARS، NEXUS_PEOPLE_CONTEXT_*،
                    NEXUS_PEOPLE_ROSTER_SCAN، GEMINI_SEARCH_*)
  تست:
    tests/test_chat_queue.py        (جدید، ۱۴)
    tests/test_chat_concurrency.py  (جدید، ۶)
    tests/test_reply_splitting.py   (جدید، ۱۰)
    tests/test_answer_shape.py      (جدید، ۱۳)
    tests/conftest.py  tests/test_awareness.py  tests/test_awareness_context.py
    tests/test_chat.py  tests/test_context_plan.py  tests/test_identity.py
    tests/test_reply_target.py  tests/test_web_search.py
  مستندات:
    AgentMD.md (§53.6، §53.7، §53.8، §53.12، §54.31)
    ChatGPT.md (§10 همگام شد)


۵) معماری جدید (خلاصه)
───────────────────────
  پیام گروهی
    → intake (بدون تغییر)
    → صف نوبت (app/chat_queue.py): قفل گفتگو + دروازهٔ همزمانی + صبر روی پنجرهٔ ما
    → خواندن زمینه (context_plan.read) → انتخاب منابع
    → ترکیب context_plan.compose:
         admin | target | people | room | awareness | state | memory | date | search
    → خواندن طول (answer_shape.read → render)
    → یادداشت مالک (فقط داده، فقط با شناسهٔ تنظیمشده)
    → مدل (chat.reply، سقف ۸۱۹۲ توکن)
    → ارسال (_send_chat): escape → تقسیم در درزهای طبیعی → منشن فقط روی پیام اول
    → ثبت در پنجرهٔ آگاهی (آنچه گفته شد) + حافظهٔ نام (people.roster)

  قواعد دستنخورده:
    - دروازههای قطعی فقط برای زیرساخت و امنیت؛ ارتباط، اقدام و گفتار کار مدل است.
    - هیچکدام از مسیرهای جدید فراخوانی مدل اضافه نکردند (answer_shape،
      people.roster، سؤالِ ابهام و تقسیم پیام همه قطعیاند).
    - حافظهٔ نام و roster «خواندنی» هستند و هیچ اختیاری نمیدهند؛ هدف را فقط
      reply_target از متادیتای تلگرام و پنجرهٔ ذخیرهشده جابهجا میکند.
    - fail-open در همهجا؛ تنها اقدام روی عضو همان محدودیت زمانی است.
    - جداسازی مستأجر (chat_id) و قواعد نگهداری دستنخورده.


۶) نتیجهٔ تستها
─────────────────
  کل مجموعه:  ۴۱۱۴ passed / ۰ failed   (۲۷۴ ثانیه)
  pyflakes:   روی همهٔ فایلهای دستخورده پاک؛ هشدارهای باقیمانده از قبل موجود
              و در فایلهایی هستند که این تغییر مالکشان نیست.
  اسکن راز:   diff ایندکسشده پاک است (بدون کلید Gemini/Tavily/توکن).

  دو نقص تستِ از قبل موجود هم هنگام راستیآزمایی اصلاح شد:
    - tests/test_web_search.py نام test_small_talk_does_not_search را دو بار
      تعریف کرده بود؛ تعریف اول بیصدا بازنویسی و هرگز collect نمیشد — یعنی
      یک تستِ گمشده، نه یک تکرار. تست سطحِ هندلر تغییر نام داده شد.
    - سه import بیاستفاده حذف شد.


۷) کامیت، شاخه و دیپلوی
────────────────────────
  شاخه:                    main
  base:                    fd4f9d6
  کامیتهای این کار (به ترتیب):
    01725c9  feat(chat): queue bursts, resolve named targets, and answer at
             the asked length            ← کد + تستها
    7892989  docs(agentmd): record the 21-section overhaul, and restore the
             missing §54.27              ← AgentMD + ChatGPT + همین فایل
    1d449d6  test(probe): cover the tag directive and the length policy in
             the live probe
    09286b1  test(probe): add the deliberately-long case that exercises the
             split
  پوش:                     origin/main و dashmo3i/main هر دو روی 09286b1
                           (با git ls-remote روی خود ریموت تأیید شد، نه با
                           خروجی git push)
  تصویر دیپلویشده:          guardbot:latest = d310e255659f
  تصویر رولبک:              guardbot:rollback-pre-1d449d6 = c89925bf3da0
                           (تصویر قبلیِ در حال اجرا، برای بازگشت فوری)

  تأیید دیپلوی:
    - کانتینر guardbot: status=running، restarts=0،
      started=2026-09-26T16:37:40Z، image=d310e255659f
    - مقادیر تنظیمات داخل کانتینر تأیید شد:
        GEMINI_CHAT_REPLY_MAX_CHARS=12000   GEMINI_CHAT_REPLY_CHARS=3500
        GEMINI_CHAT_MAX_CONCURRENCY=4       NEXUS_PEOPLE_ROSTER_SCAN=500
        NEXUS_PEOPLE_CONTEXT_ITEMS=8        GEMINI_SEARCH_MAX_RESULTS=8
        GEMINI_SEARCH_SNIPPET_CHARS=600     GEMINI_SEARCH_MAX_CHARS=3600
        max_output_tokens=8192
    - تطابق بایتبهبایت کد: همهٔ app/*.py داخل تصویرِ در حال اجرا با مخزن یکسان
      است (md5sum، فایلبهفایل).
    - لاگ راهاندازی بدون traceback؛ سه پاس آگاهی روی گروههای واقعی اجرا شد.
    - کانتینر داشبورد دستنخورده ماند (روی تصویر قدیمی خودش اجرا میشود) و
      vpn-bot هم اصلاً بخشی از این compose نیست.
    - فقط سرویس guardbot بازساخته شد؛ داشبورد بازسازی نشد.


۸) پروب زندهٔ سرتاسری (داخل کانتینر، خودپاککن)
───────────────────────────────────────────────
  ابزار: tools/probe_reply_target.py
  اجرا:  docker cp → docker exec ... python /tmp/probe_reply_target.py --real --real-long
  شناسههای ساختگی؛ همهچیز در finally پاک میشود.

  نتیجهٔ پاکسازی: cleanup_rows_left = 0 در همهٔ جدولها (authorized_groups،
  group_messages، chat_messages، people). یعنی پروب هیچ ردی روی دادهٔ واقعی
  نگذاشت.

  (الف) نقصِ گزارششدهٔ مالک — «فلانی رو تگ کن» در پاسخ به پیام خودِ نکسوس
        tag_while_reply_to_bot → reply_to = 900001490 (پیام میلاد)
                                     mention میلاد = True
        پیش از این اصلاح، پاسخ زیر پیام خودِ پرسشگر (900001500) میرفت.
        tag_plain            → reply_to = 900001490، mention میلاد = True
        tag_with_no_message  → reply_to = None (بیضمیمه، نه زیر پای پرسشگر)،
                               mention سارا = True
        tag_unknown_name     → reply_to = پیام خود پرسشگر، بدون mention
                               (هرگز هدف جعلی نمیسازد)

  (ب) رگرسیون: هیچکدام از رفتارهای قبلی هدف پاسخ تغییر نکرد
        reply_then_this / _person / _what_is_this / _what_says_it / _agreement
        / _possessive / _third_person → همه زیر پیام والد (900001480)
        reply_no_reference / plain_mention / reply_to_bot → زیر پیام خود پرسشگر
        named_person_no_reply → زیر پیام میلاد (900001490)

  (ج) سیاست طول، با مدل واقعی
        real_full_answer  → 1994 کاراکتر در یک پیام
                            (پیش از این: چند خط خلاصه)
        real_long_answer  → 3717 کاراکتر در **دو پیام** (تقسیم، نه بریدن)

  (د) یک ترن واقعی روی پاسخ به والد
        real_reply_then_this → 166 کاراکتر، پاسخ زیر پیام زهرا؛ کلمات والد
        («زعفران»/«سفر»/«عکس») به مدل رسیده بود.

  نکتهٔ provider: در همین بازه چند 503 گذرا از سمت Gemini در لاگ pool دیده شد
  (و یک rate_limited روی free tier). pool خودش failover کرد و پاسخها رسیدند؛
  این وضعیت provider-side است و ربطی به این تغییر ندارد.


۹) چه چیزی لایو است و چه چیزی در انتظار
────────────────────────────────────────
  لایو (روی همین سرور، همین حالا):
    ✔ حذف کامل پیام محدودیت نرخ + صف نوبت (app/chat_queue.py)
    ✔ هدفگیری «تگ کن» و «جواب فلانی رو بده» + منشن واقعی تلگرام
    ✔ پرسیدن «کدامیک؟» در نام مبهم، بدون فراخوانی مدل
    ✔ سیاست طول: سقف ۸۱۹۲ توکن + تقسیم پاسخ بلند به چند پیام
    ✔ app/answer_shape.py
    ✔ حافظهٔ نام اتاق (people.roster) + اسلات people در context
    ✔ username روی خطِ ترنسکریپت آگاهی
    ✔ یافتههای جستوجوی کاملتر (۸ نتیجه / ۳۶۰۰ کاراکتر)
    ✔ رفع NameError در app/rbac.py
    ✔ ۴۱۱۴ تست، ۰ خطا

  در انتظار / خارج از این کار:
    • داشبورد (Admin Control Center) مراحل M4…M8 — محصول بعدی، طبق §54.27.
    • تغییرات uncommitted در app/voice_live/* و tests/test_voice_live_* که
      متعلق به این کار نیستند و عمداً کامیت نشدند (Voice Live توسط مالک
      رها شده است؛ §54.30).
    • روتین نگهداری دیسک و سایر کارهای سرور، دستنخورده.

  پروب زندهٔ تعاملی تلگرام (لمسکردن توسط خود مالک) در این پاس انجام نشد؛
  بهجای آن پروب سرتاسریِ خودپاککن داخل کانتینر با دو ترن واقعی مدل اجرا شد
  و نتایج بالا ثبت شده است. اگر مالک بخواهد، مراحل لمس دستی:
    ۱. در گروه، روی پیام خودِ نکسوس Reply بزند و بنویسد: «میلاد رو تگ کن»
       → انتظار: پاسخ زیر پیام میلاد، با منشن واقعی میلاد.
    ۲. بنویسد: «نکسوس لطفا کامل و با جزییات توضیح بده …»
       → انتظار: پاسخ بلند (چند پیام در صورت عبور از ۳۵۰۰ کاراکتر).
    ۳. دو پیام پشتسرهم سریع بفرستد → انتظار: هیچ پیام «داری خیلی سریع
       پیام میدی» و هیچ پیام گمشدهای.


۱۰) محدودیتهای باقیمانده (صادقانه)
────────────────────────────────────
  • اسکن حافظهٔ نام با «تازگی» کراندار است: عضوی که در ۵۰۰ ردیف آخر دیده نشده
    در حافظهٔ نام نمیآید (نتیجه: یک خط زمینهٔ کمتر، نه یک خط غلط).
  • تقسیم پیام در درزهای متن (خط خالی، خط جدید، نقطه/ویرگول، فاصله) انجام
    میشود، نه در مرزهای معنایی.
  • یک توکن یکتکهٔ بلندتر از یک پیام تلگرام بهناچار بریده میشود.
  • بازنویسی فحشِ جدی با «پرامپت» انجام میشود؛ فقط بندِ پرامپت تست شده، نه
    خروجی تولیدشده.
  • تطابق نام در حافظهٔ نام «دقیق» است (مثل resolve)، پس یک نام نادرست میتواند
    یک خط زمینهٔ اشتباه اضافه کند — اما هیچ اختیاری نمیدهد و هدفِ پاسخ را
    تغییر نمیدهد.

