python-telegram-bot[job-queue]==21.6
Pillow>=10.0
# Explicit body-region detector (bundled ONNX model, CPU-only).
# Pulls onnxruntime + opencv-python-headless.
nudenet==3.4.2
# Second-stage scene classifier (REVIEW-only; it can never delete on its own).
# CPU-only torch is installed separately in the Dockerfile from the pytorch CPU
# index, so `pip install -r requirements.txt` here finds it already satisfied.
# Installing this file on a machine without that step pulls the CUDA torch
# build instead, which is much larger - use Docker for the real environment.
transformers>=4.40,<5
torch>=2.2
